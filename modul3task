Число Фибоначчи
Создайте новый файл и разместите в нем функцию для просчет числа Фибоначчи. Ниже код этой функции:
def fib(n): # пишет ряд Фибоначчи до n
	a, b = 0, 1
	while b < n:
		print(b, end=' ')
		a, b = b, a+b
	print()
Импортируйте этот файл как модуль в ваш основной файл. Выполните действия:
импортируйте целый файл;
импортируйте лишь функцию fib;
импортируйте функцию fib по названию fibonacci.
